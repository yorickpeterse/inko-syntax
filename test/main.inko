import std.env
import std.test.(Filter, Tests)

import syntax.lexers.test_inko
import syntax.test_format

class async Main {
  fn async main {
    let tests = Tests.new

    test_inko.tests(tests)
    test_format.tests(tests)

    tests.filter = Filter.from_string(env.arguments.opt(0).unwrap_or(''))
    tests.run
  }
}
